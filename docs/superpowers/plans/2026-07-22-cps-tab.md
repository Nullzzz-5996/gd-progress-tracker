# Вкладка «CPS» — план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Вкладка-тестер CPS: «Старт» запускает глобальный подсчёт нажатий (ЛКМ/↑/W/пробел), Escape/«Стоп» завершает и показывает макс CPS и CPS за лучшие 10 секунд, с живым показом во время замера.

**Architecture:** Чистый калькулятор метрик в Core; `CpsViewModel` с сеамом часов (`Func<long>`), считающий живые/итоговые метрики; абстракция `IGlobalInputMonitor` (реализация в App через низкоуровневые хуки WinAPI). Страница со своим `DispatcherTimer` для живого показа. БД не используется.

**Tech Stack:** .NET 10, WPF + WPF-UI, MVVM (CommunityToolkit.Mvvm), P/Invoke (`user32`), xUnit + FluentAssertions.

## Global Constraints

- Ветка `feature/cps-tab` от `main`. **Перед Task 1 контроллер исполнения переносит на ветку standalone-коммит build-фиксов `591faf6`** (SQLite 2.1.12, таргет `net10.0-windows10.0.19041.0`, версия 1.0.1) — иначе вернутся предупреждения NU1701/NU1903. После этого приложение таргетит `net10.0-windows10.0.19041.0`, прочие проекты — `net10.0`.
- Решение `GdTracker.slnx`. Build: `dotnet build GdTracker.slnx`. Tests: `dotnet test GdTracker.slnx`. Сборка **0 предупреждений, 0 ошибок**; вывод тестов чистый. x64 (`Directory.Build.props`).
- Захват ввода **глобальный**: хуки `WH_KEYBOARD_LL`(13)/`WH_MOUSE_LL`(14) через `SetWindowsHookEx` к `user32`, без сторонних зависимостей. Ставятся на `Start`, снимаются на `Stop`/Escape/уходе со страницы.
- Засчитываются down-переходы: ЛКМ (`WM_LBUTTONDOWN`=0x0201), ↑ (`VK_UP`=0x26), W (0x57), пробел (`VK_SPACE`=0x20). Автоповтор игнорируется (множество «нажатых» клавиш, убираем на `WM_KEYUP`). Escape (`VK_ESCAPE`=0x1B) → `StopRequested`, не клик. Все вводы — один общий поток кликов. Логировать содержимое ввода нельзя — только факты нажатий этих клавиш.
- Метрики: **MaxCps** = пик нажатий в любом окне 1 с (int); **Best10sCps** = макс нажатий в любом окне 10 с ÷ 10 (double, **null** при длительности < 10000 мс → в UI «—»); **CurrentCps** = нажатия за последнюю 1 с (int). Окна полуоткрытые `(end−размер, end]`.
- Часы — сеам `Func<long>` (монотонные мс, по умолчанию `Environment.TickCount64`); метки нажатий хранятся как мс-от-старта.
- Без сохранения (транзитно), показывается только последний результат.
- Страница как `StatsPage` (VM в конструкторе, `DataContext`, transient); пункт навигации в `MainWindow.xaml`; регистрации в `App.xaml.cs`.
- Точные имена, от которых зависят поздние задачи: `CpsResult(int MaxCps, double? Best10sCps, int TotalClicks, long DurationMs)`; `CpsCalculator.Compute(IReadOnlyList<long>, long)` / `CpsCalculator.CurrentCps(IReadOnlyList<long>, long)`; `IGlobalInputMonitor` (события `Pressed`/`StopRequested`, методы `Start`/`Stop`); `CpsViewModel` (`IsRunning`,`CurrentCps`,`MaxCps`,`TotalClicks`,`Elapsed`,`Result`,`HasResult`,`RefreshLive()`,`StartCommand`,`StopCommand`).

---

### Task 1: Core — калькулятор метрик CPS + результат + тесты

**Files:**
- Create: `src/GdTracker.Core/Models/CpsResult.cs`
- Create: `src/GdTracker.Core/Services/CpsCalculator.cs`
- Test: `tests/GdTracker.Tests/CpsCalculatorTests.cs`

**Interfaces:**
- Produces: `record CpsResult(int MaxCps, double? Best10sCps, int TotalClicks, long DurationMs)`; `static class CpsCalculator` with `CpsResult Compute(IReadOnlyList<long> pressTimesMs, long durationMs)` and `int CurrentCps(IReadOnlyList<long> pressTimesMs, long nowMs)`.

- [ ] **Step 1: Написать падающие тесты**

Create `tests/GdTracker.Tests/CpsCalculatorTests.cs`:

```csharp
using FluentAssertions;
using GdTracker.Core.Services;

namespace GdTracker.Tests;

/// <summary>Юнит-тесты чистого калькулятора метрик CPS.</summary>
public class CpsCalculatorTests
{
    // Равномерно: нажатие каждые 100 мс в течение 12 с (120 нажатий) → 10/с.
    private static long[] Uniform() => Enumerable.Range(0, 120).Select(i => (long)i * 100).ToArray();

    [Fact]
    public void Uniform_rate_gives_expected_max_and_best10()
    {
        var r = CpsCalculator.Compute(Uniform(), 12000);
        r.TotalClicks.Should().Be(120);
        r.MaxCps.Should().Be(10);
        r.Best10sCps.Should().Be(10.0);
    }

    [Fact]
    public void Burst_has_high_max_but_low_best10()
    {
        // 20 нажатий за 200 мс, заход 12 с.
        var t = Enumerable.Range(0, 20).Select(i => (long)i * 10).ToArray();
        var r = CpsCalculator.Compute(t, 12000);
        r.MaxCps.Should().Be(20);
        r.Best10sCps.Should().Be(2.0); // 20 нажатий / 10 с
    }

    [Fact]
    public void Run_shorter_than_10s_has_null_best10()
    {
        var r = CpsCalculator.Compute(new long[] { 0, 500, 1000, 1500, 2000 }, 5000);
        r.Best10sCps.Should().BeNull();
        r.MaxCps.Should().BeGreaterThan(0);
    }

    [Fact]
    public void Empty_input_is_zero()
    {
        var r = CpsCalculator.Compute(Array.Empty<long>(), 15000);
        r.MaxCps.Should().Be(0);
        r.Best10sCps.Should().Be(0.0); // ≥10 с, но нажатий нет
        r.TotalClicks.Should().Be(0);
    }

    [Fact]
    public void CurrentCps_counts_presses_in_last_second()
    {
        var t = Enumerable.Range(0, 10).Select(i => (long)i * 100).ToArray(); // 0..900
        CpsCalculator.CurrentCps(t, 900).Should().Be(10);   // окно (-100, 900]
        CpsCalculator.CurrentCps(t, 1500).Should().Be(4);   // окно (500, 1500]: 600,700,800,900
        CpsCalculator.CurrentCps(t, 5000).Should().Be(0);
    }
}
```

- [ ] **Step 2: Запустить — не компилируется**

Run: `dotnet test GdTracker.slnx --filter FullyQualifiedName~CpsCalculatorTests`
Expected: ошибка компиляции (нет `CpsCalculator`).

- [ ] **Step 3: Создать `CpsResult`**

Create `src/GdTracker.Core/Models/CpsResult.cs`:

```csharp
namespace GdTracker.Core.Models;

/// <summary>Итог замера CPS.</summary>
/// <param name="MaxCps">Пиковый CPS: наибольшее число нажатий за окно 1 с.</param>
/// <param name="Best10sCps">Лучший устойчивый CPS за окно 10 с (нажатий/10); null при заходе &lt; 10 с.</param>
/// <param name="TotalClicks">Всего засчитанных нажатий.</param>
/// <param name="DurationMs">Длительность захода, мс.</param>
public record CpsResult(int MaxCps, double? Best10sCps, int TotalClicks, long DurationMs);
```

- [ ] **Step 4: Реализовать `CpsCalculator`**

Create `src/GdTracker.Core/Services/CpsCalculator.cs`:

```csharp
using GdTracker.Core.Models;

namespace GdTracker.Core.Services;

/// <summary>
/// Чистые вычисления метрик CPS из меток времени нажатий (мс от старта, по возрастанию).
/// </summary>
public static class CpsCalculator
{
    private const long OneSecond = 1000;
    private const long TenSeconds = 10_000;

    /// <summary>Итог: пиковый CPS (окно 1 с) и лучший устойчивый (окно 10 с; null при &lt; 10 с).</summary>
    public static CpsResult Compute(IReadOnlyList<long> pressTimesMs, long durationMs)
    {
        int max1s = MaxInWindow(pressTimesMs, OneSecond);
        double? best10 = durationMs >= TenSeconds ? MaxInWindow(pressTimesMs, TenSeconds) / 10.0 : null;
        return new CpsResult(max1s, best10, pressTimesMs.Count, durationMs);
    }

    /// <summary>Текущий CPS: число нажатий в окне (nowMs − 1000, nowMs]. Метки по возрастанию.</summary>
    public static int CurrentCps(IReadOnlyList<long> pressTimesMs, long nowMs)
    {
        long from = nowMs - OneSecond;
        int count = 0;
        for (int i = pressTimesMs.Count - 1; i >= 0 && pressTimesMs[i] > from; i--)
            count++;
        return count;
    }

    /// <summary>Наибольшее число нажатий в любом окне размера window (мс), оканчивающемся на нажатии.</summary>
    private static int MaxInWindow(IReadOnlyList<long> t, long window)
    {
        int max = 0, start = 0;
        for (int end = 0; end < t.Count; end++)
        {
            while (t[end] - t[start] >= window) start++;
            int count = end - start + 1;
            if (count > max) max = count;
        }
        return max;
    }
}
```

- [ ] **Step 5: Запустить — тесты зелёные**

Run: `dotnet test GdTracker.slnx --filter FullyQualifiedName~CpsCalculatorTests`
Expected: PASS (5 тестов).

- [ ] **Step 6: Коммит**

```bash
git add src/GdTracker.Core/Models/CpsResult.cs src/GdTracker.Core/Services/CpsCalculator.cs tests/GdTracker.Tests/CpsCalculatorTests.cs
git commit -m "feat: CpsCalculator + CpsResult for CPS tab"
```

---

### Task 2: ViewModels — абстракция монитора + CpsViewModel + тесты

**Files:**
- Create: `src/GdTracker.ViewModels/IGlobalInputMonitor.cs`
- Create: `src/GdTracker.ViewModels/CpsViewModel.cs`
- Test: `tests/GdTracker.Tests/CpsViewModelTests.cs`

**Interfaces:**
- Consumes: `CpsCalculator`, `CpsResult` (Task 1).
- Produces: `interface IGlobalInputMonitor { event Action? Pressed; event Action? StopRequested; void Start(); void Stop(); }`; `class CpsViewModel : ViewModelBase` with ctors `(IGlobalInputMonitor)` and `(IGlobalInputMonitor, Func<long>)`, observable `IsRunning/CurrentCps/MaxCps/TotalClicks/Elapsed/Result`, computed `HasResult`, `void RefreshLive()`, commands `StartCommand`/`StopCommand`.

- [ ] **Step 1: Написать падающие тесты**

Create `tests/GdTracker.Tests/CpsViewModelTests.cs`:

```csharp
using FluentAssertions;
using GdTracker.ViewModels;

namespace GdTracker.Tests;

/// <summary>Тесты CpsViewModel с фейковым монитором и управляемыми часами.</summary>
public class CpsViewModelTests
{
    private sealed class FakeMonitor : IGlobalInputMonitor
    {
        public event Action? Pressed;
        public event Action? StopRequested;
        public int StartCount { get; private set; }
        public int StopCount { get; private set; }
        public void Start() => StartCount++;
        public void Stop() => StopCount++;
        public void RaisePressed() => Pressed?.Invoke();
        public void RaiseStop() => StopRequested?.Invoke();
    }

    [Fact]
    public void Start_sets_running_and_starts_monitor()
    {
        var mon = new FakeMonitor();
        long now = 0;
        var vm = new CpsViewModel(mon, () => now);

        vm.StartCommand.Execute(null);

        vm.IsRunning.Should().BeTrue();
        mon.StartCount.Should().Be(1);
        vm.Result.Should().BeNull();
        vm.HasResult.Should().BeFalse();
    }

    [Fact]
    public void Presses_counted_and_current_cps_reflects_last_second()
    {
        var mon = new FakeMonitor();
        long now = 0;
        var vm = new CpsViewModel(mon, () => now);
        vm.StartCommand.Execute(null);

        for (int i = 0; i < 5; i++) { now = i * 100; mon.RaisePressed(); } // 0..400
        now = 500; vm.RefreshLive();
        vm.CurrentCps.Should().Be(5);
        vm.TotalClicks.Should().Be(5);

        now = 2000; vm.RefreshLive();
        vm.CurrentCps.Should().Be(0);
    }

    [Fact]
    public void Stop_computes_result_and_stops_monitor()
    {
        var mon = new FakeMonitor();
        long now = 0;
        var vm = new CpsViewModel(mon, () => now);
        vm.StartCommand.Execute(null);

        for (int i = 0; i < 120; i++) { now = i * 100; mon.RaisePressed(); } // 10/с, 12 с
        now = 12000;
        vm.StopCommand.Execute(null);

        vm.IsRunning.Should().BeFalse();
        mon.StopCount.Should().Be(1);
        vm.HasResult.Should().BeTrue();
        vm.Result!.TotalClicks.Should().Be(120);
        vm.Result.MaxCps.Should().Be(10);
        vm.Result.Best10sCps.Should().Be(10.0);
    }

    [Fact]
    public void Escape_stop_request_stops_when_running()
    {
        var mon = new FakeMonitor();
        long now = 0;
        var vm = new CpsViewModel(mon, () => now);
        vm.StartCommand.Execute(null);

        now = 5000;
        mon.RaiseStop();

        vm.IsRunning.Should().BeFalse();
        vm.HasResult.Should().BeTrue();
        mon.StopCount.Should().Be(1);
    }

    [Fact]
    public void Restart_clears_previous_result()
    {
        var mon = new FakeMonitor();
        long now = 0;
        var vm = new CpsViewModel(mon, () => now);
        vm.StartCommand.Execute(null);
        now = 3000; mon.RaisePressed();
        vm.StopCommand.Execute(null);
        vm.HasResult.Should().BeTrue();

        vm.StartCommand.Execute(null);

        vm.HasResult.Should().BeFalse();
        vm.Result.Should().BeNull();
        vm.TotalClicks.Should().Be(0);
        vm.CurrentCps.Should().Be(0);
    }

    [Fact]
    public void Presses_before_start_are_ignored()
    {
        var mon = new FakeMonitor();
        long now = 0;
        var vm = new CpsViewModel(mon, () => now);

        mon.RaisePressed(); // до старта — игнор
        vm.StartCommand.Execute(null);
        now = 100; mon.RaisePressed();
        now = 200; vm.RefreshLive();

        vm.TotalClicks.Should().Be(1);
    }
}
```

- [ ] **Step 2: Запустить — не компилируется**

Run: `dotnet test GdTracker.slnx --filter FullyQualifiedName~CpsViewModelTests`
Expected: ошибка компиляции (нет `IGlobalInputMonitor`, `CpsViewModel`).

- [ ] **Step 3: Создать абстракцию монитора**

Create `src/GdTracker.ViewModels/IGlobalInputMonitor.cs`:

```csharp
namespace GdTracker.ViewModels;

/// <summary>Глобальный монитор ввода (реализуется в слое App через низкоуровневые хуки WinAPI).</summary>
public interface IGlobalInputMonitor
{
    /// <summary>Засчитанное нажатие (ЛКМ / ↑ / W / пробел).</summary>
    event Action? Pressed;

    /// <summary>Запрошено завершение замера (нажат Escape).</summary>
    event Action? StopRequested;

    /// <summary>Ставит хуки и начинает слежение.</summary>
    void Start();

    /// <summary>Снимает хуки.</summary>
    void Stop();
}
```

- [ ] **Step 4: Реализовать `CpsViewModel`**

Create `src/GdTracker.ViewModels/CpsViewModel.cs`:

```csharp
using CommunityToolkit.Mvvm.ComponentModel;
using CommunityToolkit.Mvvm.Input;
using GdTracker.Core.Models;
using GdTracker.Core.Services;

namespace GdTracker.ViewModels;

/// <summary>Вкладка «CPS»: замер скорости кликов с живым показом и итогом.</summary>
public partial class CpsViewModel : ViewModelBase
{
    private readonly IGlobalInputMonitor _monitor;
    private readonly Func<long> _nowMs;
    private readonly List<long> _presses = new();
    private long _startMs;

    public CpsViewModel(IGlobalInputMonitor monitor)
        : this(monitor, () => Environment.TickCount64) { }

    // Второй параметр не резолвится контейнером DI, поэтому в приложении выбирается
    // одноаргументный конструктор с часами по умолчанию; тесты используют этот.
    public CpsViewModel(IGlobalInputMonitor monitor, Func<long> nowMs)
    {
        _monitor = monitor;
        _nowMs = nowMs;
        _monitor.Pressed += OnPressed;
        _monitor.StopRequested += OnStopRequested;
    }

    [ObservableProperty] private bool _isRunning;
    [ObservableProperty] private int _currentCps;
    [ObservableProperty] private int _maxCps;
    [ObservableProperty] private int _totalClicks;
    [ObservableProperty] private string _elapsed = "00:00";

    [ObservableProperty]
    [NotifyPropertyChangedFor(nameof(HasResult))]
    private CpsResult? _result;

    public bool HasResult => Result is not null;

    [RelayCommand]
    private void Start()
    {
        _presses.Clear();
        Result = null;
        CurrentCps = 0;
        MaxCps = 0;
        TotalClicks = 0;
        Elapsed = "00:00";
        _startMs = _nowMs();
        IsRunning = true;
        _monitor.Start();
    }

    [RelayCommand]
    private void Stop()
    {
        if (!IsRunning) return;
        _monitor.Stop();
        IsRunning = false;

        long duration = _nowMs() - _startMs;
        var result = CpsCalculator.Compute(_presses, duration);
        MaxCps = result.MaxCps;
        TotalClicks = result.TotalClicks;
        CurrentCps = 0;
        Elapsed = Format(duration);
        Result = result;
    }

    /// <summary>Пересчёт живых метрик (зовётся таймером страницы во время замера).</summary>
    public void RefreshLive()
    {
        if (!IsRunning) return;
        long now = _nowMs() - _startMs;
        CurrentCps = CpsCalculator.CurrentCps(_presses, now);
        MaxCps = CpsCalculator.Compute(_presses, now).MaxCps;
        TotalClicks = _presses.Count;
        Elapsed = Format(now);
    }

    private void OnPressed()
    {
        if (IsRunning)
            _presses.Add(_nowMs() - _startMs);
    }

    private void OnStopRequested()
    {
        if (IsRunning)
            Stop();
    }

    private static string Format(long ms)
    {
        var ts = TimeSpan.FromMilliseconds(ms);
        return $"{(int)ts.TotalMinutes:00}:{ts.Seconds:00}";
    }
}
```

- [ ] **Step 5: Запустить — тесты зелёные**

Run: `dotnet test GdTracker.slnx --filter FullyQualifiedName~CpsViewModelTests`
Expected: PASS (6 тестов).

- [ ] **Step 6: Коммит**

```bash
git add src/GdTracker.ViewModels/IGlobalInputMonitor.cs src/GdTracker.ViewModels/CpsViewModel.cs tests/GdTracker.Tests/CpsViewModelTests.cs
git commit -m "feat: CpsViewModel + IGlobalInputMonitor abstraction"
```

---

### Task 3: App — глобальный монитор ввода (P/Invoke хуки)

**Files:**
- Create: `src/GdTracker.App/Services/GlobalInputMonitor.cs`

**Interfaces:**
- Consumes: `IGlobalInputMonitor` (Task 2).
- Produces: `sealed class GlobalInputMonitor : IGlobalInputMonitor, IDisposable`.

Юнит-тестов нет (P/Invoke). Проверка — сборка; поведение хуков проверяется запуском в Task 4.

- [ ] **Step 1: Реализовать монитор**

Create `src/GdTracker.App/Services/GlobalInputMonitor.cs`:

```csharp
using System.Diagnostics;
using System.Runtime.InteropServices;
using GdTracker.ViewModels;

namespace GdTracker.App.Services;

/// <summary>
/// Глобальный монитор ввода на низкоуровневых хуках WinAPI (WH_KEYBOARD_LL/WH_MOUSE_LL).
/// Ставится на UI-потоке (нужен цикл сообщений), события поднимаются на нём же.
/// </summary>
public sealed class GlobalInputMonitor : IGlobalInputMonitor, IDisposable
{
    private const int WH_KEYBOARD_LL = 13;
    private const int WH_MOUSE_LL = 14;
    private const int WM_KEYDOWN = 0x0100;
    private const int WM_SYSKEYDOWN = 0x0104;
    private const int WM_KEYUP = 0x0101;
    private const int WM_SYSKEYUP = 0x0105;
    private const int WM_LBUTTONDOWN = 0x0201;
    private const int VK_UP = 0x26;
    private const int VK_SPACE = 0x20;
    private const int VK_W = 0x57;
    private const int VK_ESCAPE = 0x1B;

    private delegate IntPtr HookProc(int nCode, IntPtr wParam, IntPtr lParam);

    private IntPtr _keyboardHook = IntPtr.Zero;
    private IntPtr _mouseHook = IntPtr.Zero;
    private HookProc? _keyboardProc; // держим ссылки, иначе делегаты соберёт GC
    private HookProc? _mouseProc;
    private readonly HashSet<int> _down = new();

    public event Action? Pressed;
    public event Action? StopRequested;

    public void Start()
    {
        if (_keyboardHook != IntPtr.Zero)
            return;

        _down.Clear();
        _keyboardProc = KeyboardCallback;
        _mouseProc = MouseCallback;

        using var process = Process.GetCurrentProcess();
        using var module = process.MainModule!;
        var hMod = GetModuleHandle(module.ModuleName);

        _keyboardHook = SetWindowsHookEx(WH_KEYBOARD_LL, _keyboardProc, hMod, 0);
        _mouseHook = SetWindowsHookEx(WH_MOUSE_LL, _mouseProc, hMod, 0);
    }

    public void Stop()
    {
        if (_keyboardHook != IntPtr.Zero) { UnhookWindowsHookEx(_keyboardHook); _keyboardHook = IntPtr.Zero; }
        if (_mouseHook != IntPtr.Zero) { UnhookWindowsHookEx(_mouseHook); _mouseHook = IntPtr.Zero; }
        _keyboardProc = null;
        _mouseProc = null;
        _down.Clear();
    }

    public void Dispose() => Stop();

    private IntPtr KeyboardCallback(int nCode, IntPtr wParam, IntPtr lParam)
    {
        if (nCode >= 0)
        {
            int msg = wParam.ToInt32();
            int vk = Marshal.ReadInt32(lParam); // KBDLLHOOKSTRUCT.vkCode — первое поле
            if (msg == WM_KEYDOWN || msg == WM_SYSKEYDOWN)
            {
                if (vk == VK_ESCAPE)
                    StopRequested?.Invoke();
                else if ((vk == VK_UP || vk == VK_SPACE || vk == VK_W) && _down.Add(vk))
                    Pressed?.Invoke(); // только переход «отпущена → нажата»
            }
            else if (msg == WM_KEYUP || msg == WM_SYSKEYUP)
            {
                _down.Remove(vk);
            }
        }
        return CallNextHookEx(IntPtr.Zero, nCode, wParam, lParam);
    }

    private IntPtr MouseCallback(int nCode, IntPtr wParam, IntPtr lParam)
    {
        if (nCode >= 0 && wParam.ToInt32() == WM_LBUTTONDOWN)
            Pressed?.Invoke();
        return CallNextHookEx(IntPtr.Zero, nCode, wParam, lParam);
    }

    [DllImport("user32.dll", SetLastError = true)]
    private static extern IntPtr SetWindowsHookEx(int idHook, HookProc lpfn, IntPtr hMod, uint dwThreadId);

    [DllImport("user32.dll", SetLastError = true)]
    [return: MarshalAs(UnmanagedType.Bool)]
    private static extern bool UnhookWindowsHookEx(IntPtr hhk);

    [DllImport("user32.dll")]
    private static extern IntPtr CallNextHookEx(IntPtr hhk, int nCode, IntPtr wParam, IntPtr lParam);

    [DllImport("kernel32.dll", CharSet = CharSet.Unicode, SetLastError = true)]
    private static extern IntPtr GetModuleHandle(string lpModuleName);
}
```

- [ ] **Step 2: Собрать**

Run: `dotnet build GdTracker.slnx`
Expected: 0 предупреждений, 0 ошибок.

- [ ] **Step 3: Коммит**

```bash
git add src/GdTracker.App/Services/GlobalInputMonitor.cs
git commit -m "feat: GlobalInputMonitor (low-level WinAPI hooks)"
```

---

### Task 4: App — страница CPS, навигация, DI

**Files:**
- Create: `src/GdTracker.App/Views/CpsPage.xaml`
- Create: `src/GdTracker.App/Views/CpsPage.xaml.cs`
- Modify: `src/GdTracker.App/MainWindow.xaml` (пункт навигации)
- Modify: `src/GdTracker.App/App.xaml.cs` (регистрации)

**Interfaces:**
- Consumes: `CpsViewModel` (Task 2), `GlobalInputMonitor` (Task 3), `IGlobalInputMonitor`.
- Produces: `CpsPage`.

- [ ] **Step 1: Разметка страницы**

Create `src/GdTracker.App/Views/CpsPage.xaml`:

```xml
<Page x:Class="GdTracker.App.Views.CpsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="CpsPage">
    <Grid Margin="24">
        <StackPanel VerticalAlignment="Top">
            <TextBlock Text="CPS — тест скорости кликов" FontSize="28" FontWeight="SemiBold" Margin="0,0,0,8" />
            <TextBlock Opacity="0.75" TextWrapping="Wrap" Margin="0,0,0,16"
                       Text="Считаются нажатия ЛКМ, стрелки вверх, W и пробела — глобально, даже когда окно не в фокусе." />

            <!-- Старт: виден, пока не идёт замер -->
            <Button Content="Старт" Padding="24,10" FontSize="18" HorizontalAlignment="Left"
                    Command="{Binding StartCommand}">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Visibility" Value="Visible" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsRunning}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <!-- Живой показ: виден во время замера -->
            <StackPanel Margin="0,8,0,0">
                <StackPanel.Style>
                    <Style TargetType="StackPanel">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsRunning}" Value="True">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Style>

                <TextBlock Text="{Binding CurrentCps}" FontSize="72" FontWeight="Bold" HorizontalAlignment="Center" />
                <TextBlock Text="CPS сейчас" Opacity="0.7" HorizontalAlignment="Center" Margin="0,0,0,12" />
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="{Binding Elapsed, StringFormat='Время: {0}'}" Margin="0,0,24,0" />
                    <TextBlock Text="{Binding TotalClicks, StringFormat='Нажатий: {0}'}" Margin="0,0,24,0" />
                    <TextBlock Text="{Binding MaxCps, StringFormat='Макс: {0}'}" />
                </StackPanel>
                <Button Content="Стоп" Padding="20,8" Margin="0,16,0,0" HorizontalAlignment="Center"
                        Command="{Binding StopCommand}" />
                <TextBlock Text="или нажмите Escape" Opacity="0.6" HorizontalAlignment="Center" Margin="0,6,0,0" />
            </StackPanel>

            <!-- Итог: виден после замера (когда не идёт новый) -->
            <Border Margin="0,8,0,0" Padding="16" CornerRadius="8"
                    BorderThickness="1" BorderBrush="{DynamicResource CardBorderBrush}">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding HasResult}" Value="True" />
                                    <Condition Binding="{Binding IsRunning}" Value="False" />
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Visibility" Value="Visible" />
                            </MultiDataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <StackPanel>
                    <TextBlock Text="Результат" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,8" />
                    <TextBlock FontSize="16" Text="{Binding MaxCps, StringFormat='Максимальный CPS: {0}'}" />
                    <TextBlock FontSize="16"
                               Text="{Binding Result.Best10sCps, StringFormat='CPS за лучшие 10 секунд: {0:0.0}', TargetNullValue='CPS за лучшие 10 секунд: —'}" />
                    <TextBlock Opacity="0.8" Margin="0,4,0,0" Text="{Binding TotalClicks, StringFormat='Всего нажатий: {0}'}" />
                    <TextBlock Opacity="0.8" Text="{Binding Elapsed, StringFormat='Длительность: {0}'}" />
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</Page>
```

- [ ] **Step 2: Code-behind с таймером живого показа**

Create `src/GdTracker.App/Views/CpsPage.xaml.cs`:

```csharp
using System;
using System.Windows.Controls;
using System.Windows.Threading;
using GdTracker.ViewModels;

namespace GdTracker.App.Views;

public partial class CpsPage : Page
{
    private readonly CpsViewModel _viewModel;
    private readonly DispatcherTimer _timer;

    public CpsPage(CpsViewModel viewModel)
    {
        _viewModel = viewModel;
        DataContext = viewModel;
        InitializeComponent();

        // Живой показ во время замера; RefreshLive сам ничего не делает вне замера.
        _timer = new DispatcherTimer { Interval = TimeSpan.FromMilliseconds(100) };
        _timer.Tick += (_, _) => _viewModel.RefreshLive();
        _timer.Start();

        // Уходим со страницы — остановить таймер и утилизировать VM: Dispose останавливает
        // замер (снимает хуки, если шёл) и отписывается от событий монитора (как StatsPage).
        Unloaded += (_, _) =>
        {
            _timer.Stop();
            _viewModel.Dispose();
        };
    }
}
```

- [ ] **Step 3: Пункт навигации**

Modify `src/GdTracker.App/MainWindow.xaml` — в `<ui:NavigationView.MenuItems>`, после пункта «Поиск онлайн»:

```xml
                <ui:NavigationViewItem Content="Поиск онлайн"
                                       TargetPageType="{x:Type views:OnlineSearchPage}" />
                <ui:NavigationViewItem Content="CPS"
                                       TargetPageType="{x:Type views:CpsPage}" />
```

- [ ] **Step 4: Регистрации в DI**

Modify `src/GdTracker.App/App.xaml.cs`:

Рядом с другими сервисами приложения (например после регистрации `IConfirmationService`):

```csharp
                services.AddSingleton<IConfirmationService, ConfirmationService>();
                // Глобальный монитор ввода для вкладки CPS (транзитный: живёт вместе со страницей/VM).
                services.AddTransient<GdTracker.ViewModels.IGlobalInputMonitor, GdTracker.App.Services.GlobalInputMonitor>();
```

В блоке «Страницы и их view-модели» добавить:

```csharp
                services.AddTransient<CpsPage>();
                services.AddTransient<CpsViewModel>();
```

- [ ] **Step 5: Собрать**

Run: `dotnet build GdTracker.slnx`
Expected: 0 предупреждений, 0 ошибок.

- [ ] **Step 6: Прогнать все тесты (регрессия)**

Run: `dotnet test GdTracker.slnx`
Expected: PASS (все тесты, включая новые из Task 1–2).

- [ ] **Step 7: Ручная проверка в приложении**

Запустить приложение. Проверить:
1. Появилась вкладка «CPS».
2. «Старт» скрывает себя и показывает живой блок; нажатия ЛКМ/↑/W/пробела (в т.ч. в другом окне) увеличивают «CPS сейчас», «Нажатий», «Макс», идёт таймер.
3. Escape и кнопка «Стоп» завершают: показан блок «Результат» с максимальным CPS и «CPS за лучшие 10 секунд» (или «—», если заход < 10 с).
4. Повторный «Старт» обнуляет и запускает новый заход.
5. Переход на другую вкладку во время замера не оставляет висящих хуков (кнопки/ввод в приложении работают нормально).

- [ ] **Step 8: Коммит**

```bash
git add src/GdTracker.App/Views/CpsPage.xaml src/GdTracker.App/Views/CpsPage.xaml.cs src/GdTracker.App/MainWindow.xaml src/GdTracker.App/App.xaml.cs
git commit -m "feat: CPS page + navigation item + DI registration"
```

---

## Примечания по реализации

- **Выбор конструктора DI.** `CpsViewModel` имеет конструкторы `(IGlobalInputMonitor)` и `(IGlobalInputMonitor, Func<long>)`. `Func<long>` в контейнере не зарегистрирован, поэтому DI выбирает одноаргументный конструктор (часы по умолчанию `Environment.TickCount64`); двухаргументный — только для тестов.
- **Поток событий.** Хуки ставятся из `StartCommand` (UI-поток), и низкоуровневые callbacks приходят на тот же поток при прокачке сообщений диспетчером — поэтому `_presses` читается/пишется на одном потоке без блокировок.
- **Снятие хуков при Escape.** `StopRequested` приходит внутри keyboard-callback и синхронно вызывает `Stop()` → `UnhookWindowsHookEx`. Снятие хука изнутри его собственного callback допустимо; `CallNextHookEx` вызывается с `IntPtr.Zero` и безопасен после снятия. Если на реальном запуске обнаружится нестабильность, отложить `Stop()` через `Dispatcher.BeginInvoke` в code-behind.
- **Монитор транзитный**, живёт вместе со страницей/VM; `CpsPage.Unloaded` останавливает замер (снимает хуки). Отдельного `Dispose` со стороны контейнера не требуется, так как хуки снимаются явно.
